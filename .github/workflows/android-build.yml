name: Android Release Build (GitHub Runner)

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths-ignore:
      - '**/*.md'
      - '.gitignore'
      - 'README*'
  pull_request:
    branches:
      - main

jobs:
  build-android:
    runs-on: ubuntu-latest

    env:
      EXPO_USE_COMMUNITY_AUTOLINKING: "1"
      JAVA_TOOL_OPTIONS: "-Xmx3g"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Cache Yarn
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/yarn
            **/node_modules
          key: ${{ runner.os }}-yarn-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-

      - name: Install dependencies
        run: |
          corepack enable
          yarn install --frozen-lockfile

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK components
        run: |
          sdkmanager --licenses || true
          sdkmanager \
            "platforms;android-34" \
            "build-tools;35.0.0" \
            "platform-tools"

      - name: Build Android release APK
        working-directory: android
        env:
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          ./gradlew :app:assembleRelease --stacktrace --info

      - name: Build Android debug APK (fallback)
        working-directory: android
        run: |
          ./gradlew :app:assembleDebug --stacktrace --info

      - name: Collect release APK (signed or unsigned)
        run: |
          REL_DIR=android/app/build/outputs/apk/release
          if [ -f "$REL_DIR/app-release.apk" ]; then
            cp "$REL_DIR/app-release.apk" "$REL_DIR/app-release-final.apk"
          elif [ -f "$REL_DIR/app-release-unsigned.apk" ]; then
            cp "$REL_DIR/app-release-unsigned.apk" "$REL_DIR/app-release-final.apk"
          else
            echo "No release APK found"
          fi

      - name: Upload Release APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release.apk
          path: android/app/build/outputs/apk/release/app-release-final.apk
        continue-on-error: true

      - name: Upload Debug APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug.apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
        continue-on-error: true
